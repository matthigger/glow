"""Web demo layer for glow._extra.viewer.

Serves the viewer over HTTP: a Flask landing page mounting curated baked
demos and a Zenodo-record browser (server.py), the Zenodo fetch client
(zenodo.py), the demo pre-baker (bake_demos.py), and a local pickle
validator (play.py).
"""
